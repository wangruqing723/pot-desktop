# 翻译窗口与服务设置

pot-desktop 的翻译窗口（划词/输入/截图翻译时弹出的那个窗口）与「服务设置 → 翻译」页共用同一套服务实例概念。
本文件只定义术语，不记录实现细节与方案。

## Language

**翻译服务实例**：
一份已配置好的翻译服务副本，由服务实例键唯一标识；同一个服务（如 DeepL）可以配置多个实例，各自持有独立配置。
_Avoid_: 服务、翻译渠道、provider

**服务实例键**：
`服务名@随机 id` 形式的字符串，既是实例的唯一标识，也是其配置在 store 中的键；早期版本允许省略 `@id`，插件实例的服务名以 `plugin` 开头。
_Avoid_: serviceKey、服务 id

**启用**：
实例级选项，决定该实例是否出现在翻译窗口中；关闭即在翻译窗口中完全不存在。
_Avoid_: 显示、可见

**翻译卡片**：
翻译窗口中与某个已启用实例一一对应的那张卡片，含标题栏（服务名、切换实例的下拉、折叠按钮）与结果区。
_Avoid_: 结果框、target area

**源文本提交**：
源文本被定稿并广播给所有翻译卡片的那一刻——划词/截图取词、剪贴板监听送入新文本、在源文本框按回车、点源文本框的翻译按钮等；逐字输入本身不构成提交。
_Avoid_: 输入、文本变化

**手动翻译**：
实例级选项，默认关闭；开启后该实例的翻译卡片在源文本提交时不会自动翻译，必须由用户在卡片上显式触发才翻译。
_Avoid_: 懒加载、按需翻译、延迟翻译

**未翻译状态**：
翻译卡片已收到本次提交的源文本、但尚未发起翻译的状态；每次新的源文本提交都会让手动翻译实例的卡片回到该状态。
_Avoid_: 空状态、待翻译

**命名格式**：
标识符的大小写与分隔风格，如小驼峰、大驼峰、蛇形、常量、短横线；「原样」也算其中一种，表示不做任何转换。
_Avoid_: 命名规范、变量名风格、case

**分词**：
把一段文本切成若干个词的过程，是转换成任何命名格式的前置步骤；非字母数字字符一律视为分隔符并丢弃，已有的大小写边界也构成切分点。
_Avoid_: 切词、tokenize

**复制为**：
翻译卡片上的一个即时动作：把该卡片的译文按用户当场选定的命名格式写入剪贴板，既不改变卡片显示的译文，也不保存所选格式。
_Avoid_: 格式化复制、转换复制

**快捷键目标卡片**：
翻译窗口内的快捷键所作用的那张翻译卡片，即第一个已启用实例对应的卡片；与「排在第一位的实例」不同，后者可能处于关闭状态。
_Avoid_: 首张卡片、活动卡片
